<svg xmlns="http://www.w3.org/2000/svg">
<g id="sf8" transform="translate(31808,2910)" data-frame="8. MCS and Breadline schedules: Logic Spec">
  <rect data-type="frame" x="0" y="0" width="3168" height="1550" fill="#ffffff" data-title="8. MCS and Breadline schedules: Logic Spec"/>
  <textArea id="st8" x="64" y="53" width="3040" font-size="67" font-weight="bold" font-family="noto_sans" fill="#1a1a1a">8. MCS and Breadline schedules: Logic Spec</textArea>
  <textArea id="ss8" x="64" y="162" width="3040" font-size="22" font-family="noto_sans" fill="#595959">How the A5 formula turns Planned Total into one printed row per mix, in placement order.</textArea>
  <foreignObject id="sd8" x="64" y="240" width="1600" height="900" data-type="diagram" data-title="8. MCS and Breadline schedules: Logic Spec">
flowchart TD
    classDef terminator fill:#FFFFFF,stroke:#757575,color:#313131
    classDef input fill:#B3E65F,stroke:#6E9A24,color:#2F440B
    classDef export fill:#B8ACFB,stroke:#8A7BE0,color:#231266
    classDef calc fill:#9CE6FF,stroke:#2C97BB,color:#1C4657
    classDef decision fill:#9CE6FF,stroke:#2C97BB,color:#1C4657
    classDef lookup fill:#FFB575,stroke:#CC7830,color:#542700
    classDef output fill:#DDDDD8,stroke:#8A8A7E,color:#434339
    classDef issue fill:#FFC6C6,stroke:#BD0A0A,color:#5C0A0A

    subgraph row1["1. Which runs, in which order"]
        direction LR
        s(["Planned Total ready&lt;br/&gt;on Mix-Slice-Oven"]):::terminator
        f["Keep runs with Planned above 0&lt;br/&gt;on this line"]:::calc
        line{{"Breadline sheet?"}}:::decision
        mar["Marble runs become sets of&lt;br/&gt;Pump 1 bag + Rye 1.5 bags"]:::calc
        srt["Sort runs by Placement On Scedule:&lt;br/&gt;text or blank sorts last"]:::lookup
        s --&gt; f
        f --&gt; line
        line --&gt;|"Yes"| mar
        line --&gt;|"No"| srt
        mar --&gt; srt
    end
    subgraph row2["2. One row per mix"]
        direction LR
        k["Mixes per run = ROUNDUP(bags /&lt;br/&gt;Optimal Bag), 1 if none"]:::calc
        scan["SCAN running count:&lt;br/&gt;one row per mix"]:::calc
        mcs{{"MCS sheet?"}}:::decision
        co["Add Changeover rows at placement&lt;br/&gt;above + 0.5, inside the day only"]:::calc
        il["Keep Pump and Rye rows&lt;br/&gt;alternating"]:::calc
        k --&gt; scan
        scan --&gt; mcs
        mcs --&gt;|"Yes"| co
        mcs --&gt;|"No"| il
    end
    subgraph row3["3. What the sheet prints"]
        direction LR
        att[/"Notes and attributes N:S from&lt;br/&gt;Critical Lookup Information"/]:::lookup
        bc["Bag Count: bags mixed,&lt;br/&gt;raw DSD and raw DIST per run"]:::calc
        pan[/"MCS only: pans to set out&lt;br/&gt;from Oven_Info"/]:::lookup
        e4[/"E4 start time, typed"/]:::input
        nt["No start or finish times&lt;br/&gt;are calculated"]:::issue
        out(["Printed run sheet"]):::terminator
        att --&gt; bc
        bc --&gt; pan
        pan --&gt; out
        e4 -.-&gt; nt
        nt -.-&gt; out
    end
    row1 --&gt;|"sorted runs"| row2
    row2 --&gt;|"mix rows"| row3
</foreignObject>
  <foreignObject id="doc8" x="1728" y="240" width="784" height="1206" data-type="doc">
# 8. MCS and Breadline schedules: details

## Inputs

- **Mix_Slice**: Unique Dough, Asset and Planned Total.
- **Unique Dough table**: Placement On Scedule, Optimal Bag, Optimal Time. Changeover rows carry Optimal Time 10.
- **Critical Lookup Information D:O**: notes and attributes shown in N:S.
- **Oven_Info**: # of Pans/Boxes to set out, listed in MCS F29.
- **E4**: typed first-mix start (MCS 6:45, Breadline 7:50).
- **H2 total bags**: MCS ='Mix-Slice-Oven'!O2, Breadline ='Mix-Slice-Oven'!O1.

## Formula reference

- **Keep (MCS A5)**: `keep,FILTER(ud,(pt&gt;0)*(ud&lt;&gt;"")*(as="MCS LINE"),"")`
- **Sort key**: `p,IF(x="",9999,IFERROR(--x,9999))`
- **Mixes per run**: `k,IF(bpm&gt;0,ROUNDUP(tot/bpm,0),1)`
- **One row per mix**: `cum,SCAN(0,k,LAMBDA(a,v,a+v)),r,SEQUENCE(n),idx,MAP(r,LAMBDA(v,XMATCH(v,cum,1)))`
- **Changeover placement**: `co_ord,MAP(ci,LAMBDA(v,IF(v=0,0,IFERROR(--INDEX(upl,v-1),0)+0.5)))`, kept if `okco,(co_ord&gt;MIN(ord))*(co_ord&lt;MAX(ord))`
- **Marble sets (Breadline A5)**: `sets,IF(mu="",0,ROUNDUP(mt/(pb+rb),0))`

## Worked example

- **6" Italian Rolls (MCS)**: Planned 12 bags, Optimal Bag 4, so ROUNDUP(12 / 4) = 3 mixes on rows 5 to 7, 4 bags and 20 minutes each.
- **Its Bag Count row**: 12 bags mixed against 3.641 raw DSD bags and 4.774 raw DIST bags.
- **Changeover**: the Bag Count lists a Changeover between 7" Potato Hot Dog Roll and Challah Slider Roll.
- **Marble (Breadline)**: the Marble Hearth run becomes Pump for Marble Hearth 2 bags and Rye for Marble Hearth 3 bags.
- **As saved**: MCS 47 mixes + 2 changeovers, 180 bags; Breadline 25 mixes, 54.5 bags.

## Open questions

- Should the sheet calculate start and finish times from E4 and the run minutes?
- Several runs share a placement (48, 50, 53): which should go first?
- Should the Bag Count summary move so it no longer covers Time Start, Time Finish, Temp and Operator?
- Should notes and attributes come from Unique Dough so there is one list to maintain?
</foreignObject>
</g>
</svg>
