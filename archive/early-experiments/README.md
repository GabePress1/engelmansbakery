# Archive — Early Experiments (May 2026)

One-off and experimental workflows from the first round of Claude sessions.
Kept for reference only — **do not build on these**; the real projects live in
the top-level project folders.

- `bc-to-hubspot.json`, `BC-HUbSpOT.json`, `bc-hubspot.json`,
  `Business_Central_to_HubSpot.json`, `bc-to-hubspot.md` — early BC → HubSpot
  drafts, superseded by `route21-hubspot-sync/`
- `Gabe123.json` — test workflow
- `ai-articles-email.json` — AI articles email digest experiment
- `hubspot_update_company_by_erp.js` — script to update HubSpot companies keyed
  on ERP Account Number
- `github-action-sync-to-n8n.yml` — GitHub Action that pushed workflow JSON to
  n8n on merge (not installed; move to `.github/workflows/` and set the
  `N8N_URL`/`N8N_API_KEY` repo secrets if you ever want it active)
